<p>Current revision: {{model.revision}}</p>

<button {{action 'undo' model.workspace_id}}>Undo</button>
<button {{action 'redo' model.workspace_id}}>Redo</button>

<h1>Memory blocks for {{model.name}}</h1>

<ul>
  {{#each model.memory_blocks as |block_name|}}
    <li>{{#link-to 'workspace.memory_block' model.workspace_id block_name}}{{block_name}}{{/link-to}}</li>
  {{/each}}
</ul>

{{outlet}}
