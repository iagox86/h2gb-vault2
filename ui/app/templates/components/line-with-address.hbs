<div style="position:relative" >
  <tt>
    <a href="#" onclick={{action "toggle"}}>
      {{memory-address address=address}}
    </a>
    {{yield}}
  </tt>
  {{#if open}}
    {{editor-widget address=address
                    workspace_id=workspace_id
                    block_name=block_name}}
  {{/if}}
</div>

