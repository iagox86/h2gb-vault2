
import { numberHex } from 'h2gb-ui/helpers/number-hex';
import { module, test } from 'qunit';

module('Unit | Helper | number hex');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberHex([42]);
  assert.ok(result);
});

